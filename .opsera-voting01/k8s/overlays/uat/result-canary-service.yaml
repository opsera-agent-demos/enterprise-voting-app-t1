apiVersion: v1
kind: Service
metadata:
  name: result-canary
  labels:
    app: result
    role: canary
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: result
