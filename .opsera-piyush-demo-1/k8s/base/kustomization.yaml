apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/managed-by: argocd
  app.kubernetes.io/part-of: piyush-demo-1

resources:
  - namespace.yaml
  - vote-deployment.yaml
  - vote-service.yaml
  - result-deployment.yaml
  - result-service.yaml
  - worker-deployment.yaml
  - ingress.yaml
# Note: newrelic-license secret is created dynamically by CI/CD pipeline
# ConfigMap and ServiceAccounts are in overlays (environment-specific)
